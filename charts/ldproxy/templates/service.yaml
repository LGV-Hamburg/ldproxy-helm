apiVersion: v1
kind: Service
metadata:
  name: {{ include "ldproxy.fullname" . }}
  labels:
    {{- include "ldproxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ldproxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
